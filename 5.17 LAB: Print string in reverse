while True:
    text = str(input())
    if text in ['Done', 'done', 'd']:
        break
    reverse_text = ''
    for character in text:
        reverse_text = character + reverse_text
        
    print (reverse_text)
